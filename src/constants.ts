export const KEYBORG_FOCUSIN = "keyborg:focusin";
export const KEYBORG_KEYBOARDNAVIGATION = "keyborg:keyboardnavigation";

/**
 * When a key from dismissKeys is pressed and the focus is not moved
 * during _dismissTimeout time, dismiss the keyboard navigation mode.
 */
export const DIMISS_TIMEOUT = 500;
